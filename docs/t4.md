# CoD Xe T4: Call of Duty: World at War

## Singleplayer

### CoD Xe data location

Package the T4 singleplayer data under _codxe\t4:

~~~text
_codxe\
  t4\
    codxe.json
    mods\<active_mod>\...
    zone\...
    usermaps\...
~~~

The same archive can be extracted into the game directory, a USB device root,
or the internal HDD root. The lookup order is game:\_codxe\t4,
game:\_codxe (the existing layout), Usb0:\_codxe\t4 through
Usb4:\_codxe\t4, then Hdd1:\_codxe\t4. The first existing directory
becomes the only active root for this launch. Config, mods, fastfiles,
usermaps, and loose sounds are read from that root; missing files do not
fall back to another device. Usermap fastfiles take precedence over zone
fastfiles within the selected root. USB and HDD never search an un-nested
_codxe folder; that layout applies only to the game directory.

### GSC Player Methods

#### `JumpButtonPressed()`

Returns true if the jump button is pressed.

Usage example: `if (self JumpButtonPressed()) { ... }`

#### `SecondaryOffhandButtonPressed()`

Returns true if the secondary offhand button is pressed.

Usage example: `if (self SecondaryOffhandButtonPressed()) { ... }`

#### `SprintButtonPressed()`

Returns true if the sprint button is pressed.

Usage example: `if (self SprintButtonPressed()) { ... }`

#### `MoveForwardButtonPressed()`

Returns true if the player is moving forward.

Usage example: `if (self MoveForwardButtonPressed()) { ... }`

#### `MoveBackButtonPressed()`

Returns true if the player is moving backward.

Usage example: `if (self MoveBackButtonPressed()) { ... }`

#### `MoveLeftButtonPressed()`

Returns true if the player is moving left.

Usage example: `if (self MoveLeftButtonPressed()) { ... }`

#### `MoveRightButtonPressed()`

Returns true if the player is moving right.

Usage example: `if (self MoveRightButtonPressed()) { ... }`

### GSC Client Fields

#### `noclip` _(int/bool)_

Enables noclip on the player.

Usage example:

```gsc
self.noclip = true;
self.noclip = false;
```

#### `ufo` _(int/bool)_

Enables UFO mode on the player.

Usage example:

```gsc
self.ufo = true;
self.ufo = false;
```

#### `god` _(int/bool)_

Enables god mode on the player.

Usage example:

```gsc
self.god = true;
self.god = false;
```

## Multiplayer

### Dvars

#### `noclip_brushes` _(string)_

A server-info dvar that controls which brushes have player collision disabled. It holds a space-separated list of brush indices.

Special values:

- `""` - Restores all brushes to their original collision state.
- `"*"` - Disables player collision on every brush in the clipmap.

```text
set noclip_brushes "";    // reset all brush collision
set noclip_brushes "*";   // disable all brush collision
set noclip_brushes "4 7"; // disable collision on brushes 4 and 7
```

### GSC Functions

#### `GetPlayerclipBrushesContainingPoint(vec3 <point>)`

Returns an array of playerclip brush indices whose bounds contain the given point.

Usage example:

```gsc
brushes = GetPlayerclipBrushesContainingPoint(self.origin);
```

### GSC Player Methods

#### `ButtonPressed(string <button>)`

Checks if a specific local key or button is currently pressed.

Usage example: `if (self ButtonPressed("BUTTON_A")) { ... }`

#### `SprintButtonPressed()`

Returns true if the sprint button is pressed.

Usage example: `if (self SprintButtonPressed()) { ... }`

#### `JumpButtonPressed()`

Returns true if the jump button is pressed.

Usage example: `if (self JumpButtonPressed()) { ... }`

#### `SetVelocity(vec3 <velocity>)`

Sets the player's velocity to the given 3D vector.

Usage example: `self SetVelocity((0, 0, 300));`

#### `GetForwardMove()`

Returns the player's raw forward/backward movement input.

Usage example: `forward = self GetForwardMove();`

#### `GetRightMove()`

Returns the player's raw left/right movement input.

Usage example: `right = self GetRightMove();`

#### `CloneBrushModelToScriptModel(entity <brushModelEnt>)`

Clones a brush model's collision model to a `script_model` entity.

Usage example:

```gsc
scriptModel CloneBrushModelToScriptModel(brushEntity);
```

### GSC Client Fields

#### `noclip` _(int/bool)_

Enables noclip on the player.

Usage example:

```gsc
self.noclip = true;
self.noclip = false;
```

#### `ufo` _(int/bool)_

Enables UFO mode on the player.

Usage example:

```gsc
self.ufo = true;
self.ufo = false;
```

#### `god` _(int/bool)_

Enables god mode on the player.

Usage example:

```gsc
self.god = true;
self.god = false;
```

## Example Mods

Example T4 mods are available under [`resources/t4/_codxe/mods`](/resources/t4/_codxe/mods):

- `example` - Basic script examples for singleplayer and multiplayer.
- `codjumper` - Codjumper-oriented scripts and menu examples.
